<div class="container">
<div class="row">
<div class="col-md-12 col-md-offset-1 mx-auto mt-3">
<h3>注文履歴一覧</h3>

<table class="table">

  <thead>
    <th>注文日</th>
    <th>配送先</th>
    <th>注文商品</th>
    <th>支払金額</th>
    <th>ステータス</th>
    <th>注文詳細</th>
  </thead>

  <tbody>
   <% @orders.each do |order| %>
    <td><%= order.created_at %></td>
    <td><%= order.created_at %></td>
    <td><%= order.created_at %></td>
    <td><%= order.created_at %></td>
    <td><%= order.created_at %></td>
    <td><%= order.created_at %></td>
   <% end %>
</tbody>

</table>

</div>
</div>
</div>